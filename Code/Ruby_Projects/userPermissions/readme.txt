Build a program that:
- Reads in user information that is provided
- Reads in permissions from a JSON file
- Creates a new user with the asigned permissions
- Saves the user in a new users listing file, if file doens't exist yet, it creates one.